# The application's secrets are supplied OUT OF BAND (e.g. `kubectl create secret`,
# a sealed-secret, or an external secrets operator) — never templated from values and
# never committed. This stub documents only the Secret NAME the Deployment references.
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
type: Opaque
# data: {}   # populated out of band; intentionally empty in source control
