# Dev/CI toolchain pins, applied via `pip install -c` (see .github/workflows/ci.yml).
#
# pyproject.toml declares only minimums for the dev tools, so without this file
# every CI run floats to the latest ruff/mypy/pytest release; a new tool version
# could break main with zero repo changes. Constraints keep the gate
# deterministic; bump them deliberately (and watch CI) instead.
#
# Runtime dependencies are intentionally NOT pinned here: the package is a
# reference service installed with range requirements, and the Docker image /
# SBOM capture the exact runtime versions actually shipped. The exception is
# below: the semantic stack is a runtime dependency a committed measurement
# compares against byte-for-byte, so it is pinned like a tool.
ruff==0.15.20
mypy==2.1.0
pytest==9.1.1
httpx==0.28.1
pyyaml==6.0.3
types-PyYAML==6.0.12.20250915
pytest-cov==7.1.0
coverage==7.15.2
pip-audit==2.10.1
# The semantic job's floor gate re-derives a measurement and compares it to
# the committed one, so the model stack is pinned; a silent torch or
# transformers bump must not move it.
sentence-transformers==5.6.1
torch==2.13.0
transformers==5.14.1
