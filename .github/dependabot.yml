version: 2
updates:
  # The actions are pinned to commit SHAs, so nothing updates them on its own.
  # Each bump arrives as a PR that fails the pipeline signature test until the
  # pin there is updated too, which is the review the pinning exists for.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
